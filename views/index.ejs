<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/css/index.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title>TodoList v1</title>
</head>
<body>
    <h1>Todo List</h1>
    <h4><%= date %></h4>
    <h3>Tasks:</h3>
    <form action="/addTask" method="post">
        <input type="text" name="newItem" id="newItem" placeholder="define a name for the task">
        <button type="submit" disabled><span class="material-icons">add_circle</span></button>
    </form>
    <ul>
        <% for (var item of items) { %>
            <li id=<%=item.id%>><span><%=item.item%></span> <span><a href='#' onClick=<%="removeLine('"+item.id+"')"%>><span class="material-icons">delete</span></a> <a href="#" onClick=<%="editIt('"+item.id+"')"%>><span class="material-icons">edit</span></a></span></li>
        <% } %>
    </ul>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="/static/js/index.js"></script>
</body>
</html>